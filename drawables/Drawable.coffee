class drawjs.Drawable
	
	draw: (canvas) ->
		console.assert(false, "Drawable.draw(canvas) is an absstract method; must be overwritten.")
	
	animate: (canvas) ->
		console.assert(false, "Drawable.animate(canvas) is an absstract method; must be overwritten.")